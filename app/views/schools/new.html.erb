<% provide(:title, 'Create School') %>
<h1><span>Create School</span></h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@school) do |f| %>
    	<%= render "form", f: f %>

      <%= f.submit "Create School", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>