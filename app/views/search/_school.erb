<ul class="search-school">
  <li><div class="info"><strong>+&nbsp&nbsp<%= school.name %></strong> - <%= pluralize @courses.count, "course" %> - <i><%= school.distance_from([session[:latitude], session[:longitude]]).round(2) %> miles</i></div>
  <%= will_paginate @courses %>

    <ul class="search-courses" style="display: none;">
      <%= render partial: 'search', collection: @courses, as: :course, locals: {school: school} %>
    </ul>

    <%= will_paginate @courses %>
    </li>
</ul>