<ul class="search-school">
  <li><div class="info"><strong>+&nbsp&nbsp<%= school.name %></strong> - <i><%= school.distance_from([session[:latitude], session[:longitude]]).round(2) %> miles</i> - <%= pluralize(courses.count, 'course') %></div>

    <ul class="search-courses" style="display: none;">
      <%= render partial: 'search', collection: courses, as: :course, locals: {school: school} %>
    </ul>

    </li>
</ul>